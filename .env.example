from dotenv import load_dotenv
import os
load_dotenv()

# 所有环境变量现在都从 .env 加载
# 建议在项目根目录创建 .env 文件
# 示例内容：
# SMZDM_COMMENT=你的评论内容
# SMZDM_CROWD_SILVER_PRICE=yes
# SMZDM_CROWD_KEYWORD=关键词
# BARK_KEY=你的bark推送key
# 其他所有环境变量都可以在 .env 中定义
